import { NoteBlockDirective } from './note-block.directive';

describe('NoteBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteBlockDirective();
    expect(directive).toBeTruthy();
  });
});
