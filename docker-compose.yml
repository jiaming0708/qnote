version: '3'
services:
  web:
    build: .
    ports:
      - "4200:4200"
    command: bash -c "cd qnote && npm install && npm rebuild node-sass --force && ng serve -host 0.0.0.0"
    volumes:
      - ./:/app